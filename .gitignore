# Environment
.env
.env.dev
.env.prod
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/.env.local

# Backend
backend/build/
backend/.gradle/
backend/bin/
backend/logs/
*.class
*.jar
!gradle-wrapper.jar

# 런타임 로그 (서버 실행 출력 등)
*.log

# Claude Code session files (agents·skills 디렉토리는 공유, 나머지 세션 파일은 제외)
.claude/*
!.claude/agents/
!.claude/skills/

# Uploads (runtime)
uploads/pdfs/*
uploads/hwp/*
uploads/images/*
!uploads/pdfs/.gitkeep
!uploads/hwp/.gitkeep

# Docker
docker-compose.override.yml

# Temp / local scripts (커밋 금지 — 로컬 토큰·임시 데이터 포함 가능)
/add_web_cat.py
/q_list.txt

# Build cache
frontend/tsconfig.tsbuildinfo

# Runtime storage (런타임 업로드/이미지 — 커밋 대상 아님)
backend/uploads/
frontend/images/

# 로컬 참고 자료 이미지 (커밋 대상 아님)
references/images/

# DB 백업 파일 (scripts/backup-db.sh 산출물 — 커밋 대상 아님)
/backups/
